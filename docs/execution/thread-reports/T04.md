# T04 AI Extraction Thread Report

## Plan

1. Scaffold `@handwerk/ai` within the frozen workspace and contract boundaries.
2. Implement provider-neutral transcription, visual-context, normalisation, and extraction interfaces with deterministic/replay defaults.
3. Enforce strict versioned validation, photo measurement authority, prompt isolation, and resilient execution semantics.
4. Cover the canonical fixture and adversarial failure paths, run focused gates, and record handoff evidence.

| Field                        | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Branch / worktree            | `codex/vs-t04-ai-extraction` / `/Users/mohsenjamshidi/Documents/Handwerk/.worktrees/T04`                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Base SHA / final SHA         | `7632cf2fda5069a083a5edf41d9b10b7fedd64ed` / implementation `00d8466b1194872c5c3b0694dd66a807863ca38a` (a report-only handoff commit follows)                                                                                                                                                                                                                                                                                                                                                                                           |
| Scope delivered              | `packages/ai/src/**`: provider-neutral transcription, visual context, normalisation and structured extraction; deterministic fake default; disabled live boundary; strict Zod parser; safe evidence envelope; bounded resilient runner; synthetic replay adapter. `packages/ai/fixtures/**`: seven canonical/red-team replays. `packages/ai/tests/**`: focused contract, provider, replay and resilience coverage. `packages/ai/README.md`: safety and live-adapter instructions.                                                       |
| Acceptance criteria          | Pass: deterministic fake/default and repeatability; strict `handwerk.vertical-slice.v1` output; malformed/unknown/commercial output fails closed; all facts require citations; contradictions remain unresolved; authoritative/photo-only numeric claims fail; injection remains length-delimited data; timeout/cancellation/retry/idempotency return bounded safe states; metadata omits raw content; live path requires `enabled: true`; no external call is implemented or made.                                                     |
| Tests                        | `npm test --workspace @handwerk/ai -- --reporter=verbose`: 4 files, 33 passed, 0 failed/skipped. `npm run typecheck --workspace @handwerk/ai`: pass. `npm run format:check`: pass. `npm run lint`: pass. `npm run typecheck`: pass across 4 workspaces. `npm test`: pass; web build plus 2 web and 33 AI tests. `npm audit --omit=dev --audit-level=high`: pass, 0 vulnerabilities. `git diff --check`: pass. Initial root gates failed only because this worktree lacked installed web dependencies; rerun passed after local install. |
| Security/privacy impact      | Raw transcripts and provider output are confined to provider request/validation boundaries and never copied into execution metadata or safe errors. Prompt instructions and byte-length-prefixed untrusted JSON are separate. Commercial field/key allowlisting and photo-authority rules reduce R-001/R-005 at this boundary. Fixtures are explicitly synthetic. No logs, network clients, keys, signed URLs, real customer data, or production endpoints were added.                                                                  |
| Contract/dependency requests | None. Uses frozen `@handwerk/contracts` and existing root `zod` only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Known gaps                   | Root `package-lock.json` is intentionally unchanged; `npm ci` reports missing `@handwerk/ai` until T09/coordinator reconciles the new workspace. The live provider is an interface/flagged transport boundary only and remains disabled. The in-memory idempotency cache is process-local; durable cross-process idempotency is a later persistence integration concern.                                                                                                                                                                |
| Integration notes            | Merge after T03 as planned. Adapt T03 evidence records into `ExtractionEvidence`; advance `evidenceRevision` whenever source content changes. T05 should consume only validated `ExtractionResult` facts and perform approved price-book mapping independently. T09/coordinator must regenerate the root lockfile and verify `npm ci` without changing dependency versions. Risk register entries remain coordinator-owned; R-001/R-005 require integrated T05/T10 evidence before closure.                                             |
