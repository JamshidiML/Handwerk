# T01 Thread Report

## Plan

1. Implement exact domain arithmetic, unit validation, offer transitions, revision-bound approval, optimistic concurrency, and deletion policy.
2. Add repository interfaces plus deterministic in-memory and PostgreSQL implementations with tenant-scoped access.
3. Add forward-only PostgreSQL migration and an idempotent, unmistakably synthetic canonical seed.
4. Prove the critical invariants with focused unit and database integration tests, then record exact gate evidence.

| Field                               | Evidence                                                                                                                                                                                                                                                                                                             |
| ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Branch / worktree                   | `codex/vs-t01-domain-data` / `/Users/mohsenjamshidi/Documents/Handwerk/.worktrees/T01`                                                                                                                                                                                                                               |
| Base SHA / final implementation SHA | `7632cf2fda5069a083a5edf41d9b10b7fedd64ed` / `e187acf`                                                                                                                                                                                                                                                               |
| Scope delivered                     | Exact money/quantity arithmetic, approved-price-book line construction, draft state/revision/approval rules, repositories, PostgreSQL schema/migrations, synthetic seed, and confirmed demo deletion boundary                                                                                                        |
| Acceptance criteria                 | Pass: forward-only migration reruns cleanly; synthetic seed is idempotent; tenant-scoped reads do not reveal foreign records; database rejects cross-tenant joins, photo-as-measurement authority, invented prices/units, and stale exports; commercial edits invalidate approval                                    |
| Tests                               | 52 domain unit tests, 4 database unit tests, and 11 PostgreSQL integration tests pass. Root type-check, lint, format, build, and test gates pass.                                                                                                                                                                    |
| Security/privacy impact             | Composite tenant foreign keys and scoped repositories prevent cross-organisation references. Photo authority is constrained to `CONTEXT_ONLY` in PostgreSQL. Project deletion requires confirmation, returns only private object keys for T08/media deletion, and retains only a content-free, time-bounded receipt. |
| Contract/dependency requests        | None; `pg` was already available in the approved root dependency set.                                                                                                                                                                                                                                                |
| Known gaps                          | The database package must be registered in the coordinator-reconciled lockfile by T09. Storage-byte deletion, authenticated API context, production retention/legal-hold/backup deletion, and encryption/key management remain integration or production-boundary work.                                              |
| Integration notes                   | Merge first. T08 should layer authorization/audit over these repositories; T03 should persist evidence metadata through the schema; T05/T06/T07 use pricebook, clarification, offer, and export tables.                                                                                                              |

## Verification

- `npm test --workspace @handwerk/domain`: 52 passed.
- `npm run test:unit --workspace @handwerk/db`: 4 passed.
- `env HANDWERK_TEST_DATABASE_URL=postgresql://127.0.0.1:55432/handwerk_t01_test npm run test:integration --workspace @handwerk/db`: 11 passed.
- `env DATABASE_URL=postgresql://127.0.0.1:55432/handwerk_t01_evidence npm run db:migrate --workspace @handwerk/db`: no-op after the clean-migration integration test.
- `env DATABASE_URL=postgresql://127.0.0.1:55432/handwerk_t01_evidence npm run db:seed --workspace @handwerk/db`: synthetic seed applied.
- `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm run build`, and `npm test`: passed.

## Scope Notes

Only deliberately synthetic Westblick data is seeded. No production deployment, external service call, or real customer data is introduced. The local PostgreSQL integration suite begins by recreating the `public` schema in its dedicated `handwerk_t01_test` database and never targets an application or production database.
